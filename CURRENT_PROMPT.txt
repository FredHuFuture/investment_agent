# 当前任务指令 (请复制给 Developer Agent)

如果这是你所在 Session 的第一次对话，请先阅读 `docs/architecture_v4.md` 了解全局架构和业务背景。如果之前已经读过，请直接跳过，节约 Context。

## 执行顺序

### Step 1: Task 008 (Signal Aggregator + E2E Pipeline)
请严格执行 `tasks/008_signal_aggregator.md` 的要求。

**前提**: Tasks 005-007 已完成。`agents/technical.py`, `agents/fundamental.py`, `agents/macro.py` 已存在且全部测试通过 (42 passed)。

**提示**: 请先阅读 `docs/AGENT_SYNC.md` 末尾的 Architect Review，了解架构决策和注意事项。

## 通用完成步骤
每个 Task 完成后：
1. 必须在 `docs/AGENT_SYNC.md` 中进行书面汇报！
2. 必须执行 `git add .` 和 `git commit -m "..."` 提交你的所有修改！不需要推送到远端。

> **注意**: Task 008 依赖 005-007 的 Agent 实现。所有测试必须通过 (`pytest tests/ -v`)。
