# 当前任务指令 (请复制给 Developer Agent)

如果这是你所在 Session 的第一次对话，请先阅读 `docs/architecture_v4.md` 了解全局架构和业务背景。如果之前已经读过，请直接跳过，节约 Context。

## 执行: Task 014.5 (Analysis Detail Mode)

请严格执行 `tasks/014_5_analysis_detail_mode.md` 的要求。

**前提**: Tasks 001-014 已完成 (Sprint 4 in progress)。125 tests passed, 1 skipped。

**提示**:
- 阅读 `cli/report.py` 了解现有的 `format_analysis_report()` 和 `_format_agent_detail()` -- 你需要扩展它们。
- 阅读 `cli/analyze_cli.py` 了解现有的 CLI 参数结构 -- 你只需要加 `--detail` / `-d` flag。
- 阅读 `agents/models.py` 了解 `AgentOutput` 的完整字段: `metrics` (dict), `reasoning` (str), `warnings` (list)。
- 阅读 `engine/aggregator.py` 了解 `AggregatedSignal.metrics` 中已有的聚合透明度数据: `weights_used`, `agent_contributions`, `raw_score`, `consensus_score`。
- **重要**: `format_analysis_report(signal, detail=False)` 签名必须向后兼容 -- 现有调用方不受影响。
- **重要**: 不要改变 standard mode 的任何输出 -- detail=False 的行为完全不变。
- **重要**: 不要用 em dash, 用 ASCII `--`。Windows CMD 会乱码。
- **重要**: Skip None/missing metrics -- 不要显示 "N/A" 垃圾。
- **重要**: Reasoning 文本用 `> ` prefix 和 6 空格缩进，wrap at ~58 chars。
- 测试加在 `tests/test_009_cli_report.py` 中，4 个新测试。
- 不需要新文件，不需要新依赖，不需要数据层变更。纯展示层改动。

## 通用完成步骤
Task 完成后：
1. 必须在 `docs/AGENT_SYNC.md` 中进行书面汇报！
2. 必须执行 `git add .` 和 `git commit -m "..."` 提交你的所有修改！不需要推送到远端。

> **注意**: 所有测试必须通过 (`pytest tests/ -v`)。当前基线: 125 passed, 1 skipped, 0 failed。
