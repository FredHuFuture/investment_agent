# 当前任务指令 (请复制给 Developer Agent)

如果这是你所在 Session 的第一次对话，请先阅读 `docs/architecture_v4.md` 了解全局架构和业务背景。如果之前已经读过，请直接跳过，节约 Context。

## 执行顺序

### Step 1: Task 008.5 (Drift Analyzer Enhancement)
请严格执行 `tasks/008_5_drift_enhancement.md` 的要求。

**前提**: Tasks 001-008 已完成。`engine/drift_analyzer.py` 已存在且测试通过 (57 passed)。

**提示**: 这是对现有 `DriftAnalyzer` 类的增量增强，不是新建类。确保向后兼容。

### Step 2: Task 009 (CLI Analysis Report)
完成 Task 008.5 后，请执行 `tasks/009_cli_report.md`。

**前提**: Task 008.5 已完成。`engine/pipeline.py` 和 `engine/aggregator.py` 已存在。

**提示**: 阅读现有 `cli/portfolio_cli.py` 了解 CLI 模式。阅读 `agents/technical.py`, `agents/fundamental.py`, `agents/macro.py` 中的 metrics 字典了解各 agent 输出哪些 key。报告格式要防御性编程——metrics 中任何 key 缺失都不能 crash。

## 并行策略
Task 008.5 和 Task 009 **不能并行**。009 的 CLI 可能会用到 008.5 的 drift summary（如果你选择在 CLI 中展示 drift 统计的话），但 009 的核心功能不依赖 008.5。如果你选择先做 009 再做 008.5 也可以，但推荐先 008.5。

## 通用完成步骤
每个 Task 完成后：
1. 必须在 `docs/AGENT_SYNC.md` 中进行书面汇报！
2. 必须执行 `git add .` 和 `git commit -m "..."` 提交你的所有修改！不需要推送到远端。

> **注意**: 所有测试必须通过 (`pytest tests/ -v`)。当前基线: 57 passed, 2 skipped, 0 failed。
